**Ribbon** — the brand's signature scrolling marquee band, used full-bleed between sections.

```jsx
<Ribbon text="Build · Rest · Rise" tone="cobalt" />
```

Props: `text`, `separator`, `tone` (cobalt/terracotta/olive/teal/ink), `speed` (sec/loop), `count`. Always full width; place directly between page sections.
