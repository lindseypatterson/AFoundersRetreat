**Input** — labeled text field, editorial underline by default.

```jsx
<Input label="Email" type="email" placeholder="you@studio.com" />
<Input label="Full name" variant="box" />
```

Props: `label`, `variant` (underline/box), `hint`, `error`, `disabled`, plus native input attrs.
