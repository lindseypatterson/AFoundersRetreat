**NavBar** — editorial site header. `stacked` = centered script wordmark above a boxed, divider-ruled link row; `inline` = left brand / right links (+ optional `cta`).

```jsx
<NavBar brand="The Founders' Edit" active="Retreats"
  links={['Home','About','Retreats','Journal','Contact']} onNavigate={setPage} />
```
