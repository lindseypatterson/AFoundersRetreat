**Footer** — dark teal footer with script wordmark, tagline, and link columns.

```jsx
<Footer columns={[{title:'Explore', links:['Retreats','Journal','About']}]} />
```

Props: `brand`, `tagline`, `columns` `[{title,links}]`, `note`.
